-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
# Покрытие меряем не ради процента, а чтобы видеть непокрытое: у `repo.py`,
# `texts.py` и `keyboards.py` своих тестовых файлов нет вовсе
pytest-cov>=6.0
# Линтер и форматтер в одном. `.ruff_cache/` лежал в `.gitignore` с самого
# начала — ruff гоняли руками, но нигде не закрепляли
ruff>=0.16
# Код писался под статическую проверку (`Mapped[...]` везде, единственный
# `# type: ignore` в config.py), но сама проверка не запускалась ни разу
mypy>=1.13
